# 니혼줍줍 검토·수정·승인·품질 확인 기능 명세

## 1. 목표

콘텐츠 담당자가 초안을 제출하면 검토자가 원문을 훼손하지 않고 수정 의견을 남기고, 작성자가 의견별로 반영 여부를 결정한 뒤, 승인권자가 품질 기준을 확인하고 최종 승인할 수 있어야 한다.

핵심 원칙은 다음과 같다.

- 수정 전 원본과 모든 이력을 보존한다.
- 의견, 실제 수정, 승인 결정을 서로 구분한다.
- 승인 시점의 버전을 고정해 이후 변경과 섞이지 않게 한다.
- 자동 품질 검사는 승인 판단을 돕되 사람의 결정을 대신하지 않는다.
- 현재 상태와 다음 행동을 한 화면에서 알 수 있게 한다.

## 2. 역할과 권한

| 역할 | 주요 권한 |
| --- | --- |
| 작성자 | 초안 작성, 수정 제안 반영·거절, 새 검토 요청 |
| 검토자 | 구간별 의견·수정 제안, 변경 요청, 검토 완료 |
| 승인자 | 최종 승인, 반려, 승인 취소 |

- 한 사용자가 여러 역할을 가질 수 있으나, 본인이 마지막으로 수정한 버전을 스스로 최종 승인할 때는 확인 경고를 표시한다.
- 이미 승인된 버전은 직접 덮어쓰지 않는다. 수정하면 새 버전과 새 검토 회차를 만든다.

## 3. 상태 모델

`초안 → 검토 요청 → 검토 중 → 변경 요청 → 재검토 요청 → 승인 완료`

예외 상태:

- `검토 요청 취소`: 작성자가 검토 시작 전 회수
- `반려`: 현재 방향으로 승인 불가. 사유 입력 필수
- `승인 취소`: 승인 후 중대한 오류 발견. 사유와 취소자 기록 필수

상태 전환 규칙:

1. 미해결 필수 의견이 있으면 승인할 수 없다.
2. 품질 검사 중 차단 항목이 실패하면 승인할 수 없다.
3. 승인 직전에 검토한 버전과 현재 버전이 다르면 승인을 중단하고 차이를 보여준다.
4. 승인 후 변경은 승인 상태를 유지하지 않고 새 초안으로 분기한다.

## 4. 검토 화면

### 기본 구성

- 상단: 콘텐츠명, 버전, 상태, 담당자, 마지막 변경 시각
- 중앙: 콘텐츠 미리보기 또는 편집 영역
- 우측: 의견 목록과 필터
- 하단 고정 영역: 현재 사용자가 할 수 있는 다음 행동 1~2개

### 의견 유형

- `필수 수정`: 해결 전 승인 불가
- `제안`: 작성자가 반영 또는 거절 가능
- `질문`: 답변 후 검토자가 해결 처리
- `칭찬/참고`: 승인 차단 없음

모든 의견에는 작성자, 작성 시각, 대상 버전, 대상 구간, 유형, 처리 상태를 저장한다.

### 수정 제안

- 원문과 제안문을 나란히 보여준다.
- `반영`, `거절`, `일부 반영`을 제공한다.
- 거절 또는 일부 반영 시 짧은 사유를 남길 수 있다.
- 반영 직전에 충돌 여부를 검사한다. 제안 이후 원문이 바뀌었으면 자동 반영하지 않고 최신 문맥과 차이를 보여준다.
- 일괄 반영은 서로 겹치지 않고 충돌이 없는 제안에만 허용한다.

## 5. 변경 이력과 비교

- 저장할 때마다 작성자, 시각, 변경 요약을 포함한 버전을 만든다.
- 두 버전을 선택해 추가·삭제·수정 내용을 비교할 수 있어야 한다.
- 승인 버전, 현재 버전, 직전 검토 요청 버전을 빠르게 선택할 수 있게 한다.
- 의견은 생성 당시 버전에 고정하되, 최신 버전에서 대상 위치를 찾을 수 있으면 연결 상태를 표시한다.
- 승인 버전은 읽기 전용 스냅샷으로 보존한다.

## 6. 승인 경험

승인 패널에는 다음만 우선 표시한다.

- 검토 대상 버전
- 미해결 필수 의견 수
- 품질 검사 결과 요약
- 승인 이후 변경 여부
- 승인 또는 반려 버튼

승인 확인 문구:

> 현재 표시된 버전을 최종본으로 승인합니다. 이후 수정하면 새 검토가 필요합니다.

반려 시에는 사유와 담당자를 필수로 지정한다. 승인과 반려 모두 활동 이력에 남기고, 대상 버전과 검사 결과를 함께 보존한다.

## 7. 품질 확인

품질 검사는 `통과`, `주의`, `실패`, `검사 불가`로 구분한다.

### 차단 검사

- 필수 입력 누락
- 빈 콘텐츠 또는 불러올 수 없는 미디어
- 지정된 출력 규격 불일치
- 깨진 링크나 참조 파일 누락
- 승인 버전과 검사 대상 버전 불일치

### 경고 검사

- 제목·설명 길이 초과 또는 부족
- 맞춤법·띄어쓰기 의심 항목
- 중복 문장 또는 반복 구간
- 금칙어·민감 표현 후보
- 자막 안전 영역 침범, 너무 빠른 자막, 오디오 과대·과소 등 매체별 기준

각 검사 결과는 문제 위치, 이유, 해결 방법, 재검사 버튼을 제공한다. `검사 불가`는 통과로 간주하지 않는다.

## 8. 알림

다음 이벤트에서만 알린다.

- 검토 요청을 받음
- 필수 수정이 새로 등록됨
- 내 제안이 반영 또는 거절됨
- 재검토가 요청됨
- 최종 승인 또는 반려됨
- 승인 후 중대한 품질 문제가 발견됨

단순 저장, 자동 검사 통과, 이미 확인한 활동은 알림을 만들지 않는다.

## 9. 감사 기록

다음 항목은 삭제하지 않고 기록한다.

- 상태 변경 전후 값
- 수정 제안 생성·수정·반영·거절
- 의견 해결·재개
- 품질 검사 실행 결과와 규칙 버전
- 승인·반려·승인 취소 및 사유

## 10. MVP 우선순위

### P0

- 상태 전환과 역할별 권한
- 구간 의견 및 필수 수정
- 수정 제안 개별 반영·거절
- 버전 비교와 승인 버전 고정
- 차단 품질 검사
- 승인·반려 이력

### P1

- 일부 반영과 충돌 해결
- 검사 결과에서 문제 위치로 이동
- 알림 묶음 처리
- 승인 취소와 새 검토 회차

### P2

- 일괄 제안 반영
- 사용자별 검토 필터 저장
- 품질 규칙 세트 관리
- 검토 소요 시간과 반복 수정 횟수 분석

## 11. 완료 조건

- 필수 의견이 남은 상태에서는 모든 경로에서 승인이 차단된다.
- 검토 요청 후 내용이 바뀌면 승인자가 변경 사실과 차이를 확인한다.
- 제안 반영·거절 후 원문, 제안문, 결정 사유가 이력에 남는다.
- 승인된 버전을 수정해도 기존 승인 스냅샷은 변하지 않는다.
- 품질 검사 실패 원인과 해결 위치를 사용자가 찾을 수 있다.
- 새로고침, 동시 편집, 재로그인 후에도 상태와 이력이 유지된다.
- 키보드만으로 의견 탐색, 제안 결정, 승인 패널 접근이 가능하다.
- 색상만으로 상태를 구분하지 않고 텍스트와 아이콘을 함께 사용한다.

## 12. 핵심 테스트 시나리오

1. 작성자가 초안을 제출하고 검토자가 필수 수정과 제안을 각각 등록한다.
2. 작성자가 제안을 반영하고 필수 수정을 완료한 뒤 재검토를 요청한다.
3. 검토자가 이전 버전과 현재 버전의 차이를 확인하고 의견을 해결한다.
4. 품질 검사 실패 상태에서 승인 버튼이 차단되는지 확인한다.
5. 문제를 고친 뒤 재검사하고 승인한다.
6. 승인 후 내용을 수정해 새 초안과 새 검토 회차가 생성되는지 확인한다.
7. 두 사용자가 같은 제안을 동시에 처리할 때 중복 반영되지 않는지 확인한다.
