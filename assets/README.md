# Project Assets

이 폴더는 니혼줍줍 프로젝트가 관리하는 로컬 미디어 저장소의 루트다.

실제 애플리케이션은 `docs/05_asset/README.md`의 계약에 따라 `originals`, `derivatives`, `records`, `staging`, `trash`를 만든다. 원본이나 생성 결과물을 소스 코드처럼 임의로 덮어쓰지 않는다.

현재는 기술 스택 확정 전이므로 실제 미디어 파일과 런타임 인덱스는 추가하지 않았다.
